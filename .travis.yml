sudo: required

services:
  - docker

before_install:
  - docker build -t WTsotetzo/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true WTsotetzo/react-test npm test

after_success:
  - docker build -t WTsotetzo/multi-client ./client
  - docker build -t WTsotetzo/multi-nginx ./nginx
  - docker build -t WTsotetzo/multi-server ./server
  - docker build -t WTsotetzo/multi-worker ./worker
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  - docker push WTsotetzo/multi-client
  - docker push WTsotetzo/multi-nginx
  - docker push WTsotetzo/multi-server
  - docker push WTsotetzo/multi-worker